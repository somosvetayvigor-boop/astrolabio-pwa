import { createClient } from '@supabase/supabase-js'; const supabase = createClient('%NEXT_PUBLIC_SUPABASE_URL%', '%SUPABASE_SERVICE_ROLE_KEY%'); supabase.from('reading_progress').select('*').limit(1).then(console.log).catch(console.error);  
